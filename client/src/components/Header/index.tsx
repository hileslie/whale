import { Layout } from 'antd';
import React from 'react';

const { Header } = Layout;

const HeaderContent = () => {
  return (
    <Header className="header">
      <div className="logo" />
    </Header>
  )
}

export default HeaderContent;
